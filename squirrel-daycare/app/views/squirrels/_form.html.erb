<%= form_for(@squirrel) do |f| %>
  <% if @squirrel.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@squirrel.errors.count, "error") %> prohibited this squirrel from being saved:</h2>

      <ul>
      <% @squirrel.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :breed %><br>
    <%= f.text_field :breed %>
  </div>
  <div class="field">
    <%= f.label :age %><br>
    <%= f.number_field :age %>
  </div>
  <div class="field">
    <%= f.label :owner %><br>
    <%= f.text_field :owner %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
